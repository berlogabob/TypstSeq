// tylog-helper-version: 5
// tylog-package: 0.1.0
#import "packages/tylog/0.1.0/lib.typ" as package
#import "theme.typ" as theme

#let note = package.note.with(transform: theme.document)
#let ref-note = package.ref-note
#let tag = package.tag
#let date-ref = package.date-ref
#let attachment = package.attachment
#let task = package.task
#let document = theme.document

